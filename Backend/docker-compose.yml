version: '3.8'

services:
  api:
    build: .
    container_name: resumesaathi-api
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    env_file:
      - .env
